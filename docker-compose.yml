services:
  db:
    image: postgres:15
    container_name: gatekeeper_db
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - gatekeeper_db_data:/var/lib/postgresql/data

volumes:
  gatekeeper_db_data: